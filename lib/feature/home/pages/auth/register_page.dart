import 'package:flutter/material.dart';

class RegisteryPage extends StatelessWidget {
  const RegisteryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}